*  Install Git Bash locally
*  Ensure port 22 or 443 are open
*  Open bash, make sure in home directory
*  mkdir datascience
*  cd datascience
*  git init
*  git config --global user.name "KungerPDX"
*  git config --global user.email "kunger@we-worldwide.com"
*  git config --list
*  git remote add origin https://github.com/KungerPDX/datacience.git
*  git pull origin master
*  <create file>
*  git add <file>
*  git commit -m "Added file"
*  git push --set-upstream origin master
   *  Subsequently, just git push






